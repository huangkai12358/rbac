<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ymjrhk.rbac.mapper.RolePermissionMapper">
    <insert id="batchInsert">
        insert into sys_role_permission (role_id, permission_id, create_user_id) VALUES
        <foreach collection="relations" item="relation" separator=",">
            (#{relation.roleId}, #{relation.permissionId}, #{relation.createUserId})
        </foreach>
    </insert>

    <select id="selectPermissionsByRoleId" resultType="com.ymjrhk.rbac.vo.PermissionVO">
        select p.permission_id,
               p.permission_name,
               p.permission_display_name,
               p.description,
               p.status,
               p.type,
               p.parent_id,
               p.path,
               p.method,
               p.sort
        from sys_role_permission rp
                 join sys_permission p on rp.permission_id = p.permission_id
        where rp.role_id = #{roleId}
          and p.status = 1
    </select>

    <select id="selectPermissionIdsByRoleIdAndStatus" resultType="java.lang.Long">
        select p.permission_id
        from sys_role_permission rp
                 join sys_permission p on rp.permission_id = p.permission_id
        where rp.role_id = #{roleId}
          and p.status = #{status}
    </select>

    <delete id="deleteByRoleIdAndPermissionIds">
        delete from sys_role_permission
        where role_id = #{roleId}
        and permission_id in
        <foreach collection="permissionIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

</mapper>