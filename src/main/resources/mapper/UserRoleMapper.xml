<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ymjrhk.rbac.mapper.UserRoleMapper">
    <insert id="batchInsert">
        insert into sys_user_role (user_id, role_id, create_user_id) VALUES
        <foreach collection="relations" item="relation" separator=",">
            (#{relation.userId}, #{relation.roleId}, #{relation.createUserId})
        </foreach>
    </insert>

    <select id="selectRolesByUserId" resultType="com.ymjrhk.rbac.vo.RoleVO">
        select r.role_id,
               r.role_name,
               r.role_display_name,
               r.description,
               r.status
        from sys_user_role ur
        join  sys_role r on ur.role_id = r.role_id
        where ur.user_id = #{userId}
    </select>

    <select id="userHasRole" resultType="boolean">
        select exists (
            select 1
            from sys_user_role ur
                     join sys_role r on ur.role_id = r.role_id
            where ur.user_id = #{userId}
              and r.role_name = #{roleName}
        )
    </select>
</mapper>